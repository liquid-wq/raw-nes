#pragma once

// Entspricht GUI_BUILD in rawnes_gui_v3_final.py -- bei jeder Aenderung
// hochzaehlen (dort wie hier).
constexpr int kGuiBuild = 46; // C++-Portierung startet bei 1, unabhaengige
                            // Zaehlung von der Python-Version (GUI_BUILD 19)
